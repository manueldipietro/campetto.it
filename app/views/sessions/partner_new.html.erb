<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - Campetto</title>
    <%= stylesheet_link_tag 'administrator_login', media: 'all' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
</head>
<body>
    <div class="admin-login-container">
        <h1>Autenticazione Partner</h1>
        <%= form_for(:session, url: partner_log_in_path, html: { class: 'admin-login-form' }) do |f| %>
            
            <%= f.label :email, "E-mail:"%>
            <%= f.email_field :email, class: "form-control" %>

            <%= f.label :password, "Password:" %>
            <%= f.password_field :password, class: "form-control" %>
            <%= link_to "Password dimenticata?", new_partner_password_reset_path %>

            <%= f.label :remember_me, class: "checkbox inline" do %>
                <%= f.check_box :remember_me %>
                <span>Ricordami</span>
            <% end %>

            <%= f.submit "Accedi", class: "btn btn-primary",name: "loginPartnerButton" %>
        <% end %>

        <div>
            <p>Non sei ancora un nostro Parnter? <%= link_to "Entra qui!", partner_sign_up_path %></p>
        </div>
    </div>
</body>
</html>